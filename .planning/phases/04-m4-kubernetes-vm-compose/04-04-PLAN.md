---
phase: 04-m4-kubernetes-vm-compose
plan: 04
title: Engine Integration of New Collectors
goal: Wire Kubernetes, libvirt, and Compose collectors into engine.rs and SnapshotDomains
type: execute
wave: 2
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - crates/helm-monitor/src/snapshot.rs
  - crates/helm-monitor/src/engine.rs
autonomous: true
requirements: [REQ-kubernetes-collector, REQ-libvirt-collector, REQ-compose-collector]
must_haves:
  truths:
    - "SnapshotDomains struct includes kubernetes, libvirt, and compose fields"
    - "engine.rs collect_snapshot() invokes all three collectors in parallel via tokio::join!()"
    - "Collectors are unwrapped with unwrap_or_default() for graceful degradation"
    - "Integration test passes: cargo test collect_snapshot"
  artifacts:
    - path: crates/helm-monitor/src/snapshot.rs
      provides: SnapshotDomains model update
      pattern: "pub struct SnapshotDomains.*kubernetes.*libvirt.*compose"
    - path: crates/helm-monitor/src/engine.rs
      provides: engine orchestration update
      pattern: "tokio::join!.*KubernetesCollector.*LibvirtCollector.*ComposeCollector"
  key_links:
    - from: "engine.rs::collect_snapshot()"
      to: "SnapshotDomains { kubernetes, libvirt, compose, ... }"
      via: "struct initialization"
    - from: "tokio::join!()"
      to: "three new collectors"
      via: "parallel execution"
---

<objective>
Integrate the three new collectors (Kubernetes, libvirt, Compose) into the engine orchestration and snapshot data model.

Purpose: Make new infrastructure data available to the monitoring engine and UI.

Output: Updated `snapshot.rs` and `engine.rs` with integrated collector calls and SnapshotDomains fields.
</objective>

<execution_context>
@.planning/phases/04-m4-kubernetes-vm-compose/04-PATTERNS.md
@crates/helm-monitor/src/engine.rs
@crates/helm-monitor/src/snapshot.rs (lines 426–440 for SnapshotDomains struct)
</execution_context>

<context>
**Engine reference:** `engine.rs` lines 6–50 show the pattern:
- Import collectors
- Declare tokio::join!() with all collector calls
- Destructure results
- Unwrap with unwrap_or_default()
- Initialize SnapshotDomains struct with all fields

**Snapshot model:** `snapshot.rs` lines 426–440 show SnapshotDomains struct with existing fields (host, load, disks, etc.)

**Wave 2 dependency:** This plan depends on 04-01, 04-02, 04-03 completing first so structs and collectors are available to import.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add kubernetes, libvirt, compose fields to SnapshotDomains in snapshot.rs</name>
  <files>crates/helm-monitor/src/snapshot.rs</files>
  <action>
Update `crates/helm-monitor/src/snapshot.rs` around line 426–440:
- Locate `pub struct SnapshotDomains { pub host: HostIdentity, pub load: LoadSnapshot, ... }`
- Add three new fields before closing brace:
  ```
  pub kubernetes: KubernetesSnapshot,
  pub libvirt: LibvirtSnapshot,
  pub compose: ComposeSnapshot,
  ```
- Ensure KubernetesSnapshot, LibvirtSnapshot, ComposeSnapshot are imported from their respective modules:
  ```
  use crate::collectors::kubernetes::KubernetesSnapshot;
  use crate::collectors::libvirt::LibvirtSnapshot;
  use crate::collectors::compose::ComposeSnapshot;
  ```
- Verify struct derives properly (Debug, Clone, Serialize, Deserialize if the parent does)
  </action>
  <verify>
    <automated>grep -A 20 "pub struct SnapshotDomains" crates/helm-monitor/src/snapshot.rs | grep -c "kubernetes\|libvirt\|compose"</automated>
  </verify>
  <done>Three new fields present in SnapshotDomains, imports added, compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Add collector imports to engine.rs</name>
  <files>crates/helm-monitor/src/engine.rs</files>
  <action>
Update imports at top of `engine.rs` (around lines 6–11):
- Add to use statements:
  ```
  use crate::collectors::{
      ..., 
      KubernetesCollector, 
      LibvirtCollector, 
      ComposeCollector,
  };
  ```
- Check existing imports and add alphabetically
- Verify imports compile: cargo build --lib -p helm-monitor
  </action>
  <verify>
    <automated>grep "KubernetesCollector\|LibvirtCollector\|ComposeCollector" crates/helm-monitor/src/engine.rs | wc -l</automated>
  </verify>
  <done>All three collectors imported in engine.rs</done>
</task>

<task type="auto">
  <name>Task 3: Add three collectors to tokio::join!() in collect_snapshot()</name>
  <files>crates/helm-monitor/src/engine.rs</files>
  <action>
Locate `pub async fn collect_snapshot(profile: MonitorProfile) -> SystemSnapshot` (around line 20–50):
- Find the `tokio::join!(...)` macro call that orchestrates collectors
- Example current structure (lines 36–39):
  ```rust
  let (
      host_result,
      load_result,
      disks_result,
      // ... other results
  ) = tokio::join!(
      HostCollector.collect(profile),
      LoadCollector.collect(profile),
      DisksCollector.collect(profile),
      // ... other collectors
  );
  ```
- Add three new result variables to destructure:
  ```
  kubernetes_result,
  libvirt_result,
  compose_result,
  ```
- Add three new collector calls inside tokio::join!():
  ```
  KubernetesCollector.collect(profile),
  LibvirtCollector.collect(profile),
  ComposeCollector.collect(profile),
  ```
- Maintain order (alphabetically or append at end, consistently)
  </action>
  <verify>
    <automated>grep -n "KubernetesCollector.collect\|LibvirtCollector.collect\|ComposeCollector.collect" crates/helm-monitor/src/engine.rs | wc -l</automated>
  </verify>
  <done>All three collectors called in tokio::join!(), results destructured</done>
</task>

<task type="auto">
  <name>Task 4: Unwrap results and initialize SnapshotDomains with new fields</name>
  <files>crates/helm-monitor/src/engine.rs</files>
  <action>
Locate the code after tokio::join!() that unwraps results (around lines 52–80):
- Example pattern (lines 52–65):
  ```rust
  let host = host_result.unwrap_or_default();
  let load = load_result.unwrap_or_default();
  let disks = disks_result.unwrap_or_default();
  // ...
  ```
- Add three new unwrap lines:
  ```
  let kubernetes = kubernetes_result.unwrap_or_default();
  let libvirt = libvirt_result.unwrap_or_default();
  let compose = compose_result.unwrap_or_default();
  ```
- Locate the `SnapshotDomains { ... }` struct initialization (around lines 66–80)
- Add three new fields to the initialization:
  ```
  kubernetes,
  libvirt,
  compose,
  ```
- Maintain field order (alphabetically or append at end, consistently)
  </action>
  <verify>
    <automated>grep -n "unwrap_or_default()" crates/helm-monitor/src/engine.rs | grep -c "kubernetes\|libvirt\|compose"</automated>
  </verify>
  <done>Results unwrapped, SnapshotDomains struct initialized with all three new fields</done>
</task>

<task type="auto">
  <name>Task 5: Integration test for collect_snapshot with new collectors</name>
  <files>crates/helm-monitor/src/engine.rs</files>
  <action>
Add a test at end of engine.rs or in tests/ directory:
- `#[tokio::test] async fn test_collect_snapshot_includes_new_domains()`
- Call `collect_snapshot(MonitorProfile::Standard)` (or equivalent engine method)
- `SystemSnapshot.domains` is a `SnapshotDomains` struct — access new fields via `result.domains.kubernetes`, `result.domains.libvirt`, `result.domains.compose`
- Assert that the fields compile and are accessible (field existence is the key test since binaries may not be present in CI)
- Example: `let _k = &result.domains.kubernetes; let _l = &result.domains.libvirt; let _c = &result.domains.compose;`
- New snapshot structs must derive or impl `Default` for `unwrap_or_default()` to compile
- Run: `cargo test -p helm-monitor collect_snapshot`
  </action>
  <verify>
    <automated>cargo test -p helm-monitor collect_snapshot --lib 2>&1 | grep "test result:"</automated>
  </verify>
  <done>Integration test passes, SnapshotDomains includes all three new fields</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>SnapshotDomains with kubernetes/libvirt/compose fields, engine.rs with three new collector calls in tokio::join!(), unwrap and initialization</what-built>
  <how-to-verify>
1. Full gate: `cargo fmt --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace --all-targets`
2. Verify snapshot.rs: grep for "pub kubernetes:", "pub libvirt:", "pub compose:" in SnapshotDomains
3. Verify engine.rs: grep for three collectors in tokio::join!() and unwrap_or_default()
4. Integration test passes: no errors in collect_snapshot test
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None specific to integration — security handled by individual collectors.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|-----------|
| (inherited from 04-01, 04-02, 04-03) | — | — | — | — |
</threat_model>

<verification>
Per-task gate passes; integration test confirms collectors wired correctly.
</verification>

<success_criteria>
- SnapshotDomains includes kubernetes, libvirt, compose fields
- All three collectors called in tokio::join!() in parallel
- Results unwrapped with unwrap_or_default()
- Struct initialization includes all three fields
- Integration test passes
- Full workspace gate passes
</success_criteria>

<output>
Create `.planning/phases/04-m4-kubernetes-vm-compose/04-04-SUMMARY.md` after task completion
</output>
