---
phase: 04-m4-kubernetes-vm-compose
plan: 01
title: Kubernetes Collector Implementation
goal: Build kubectl wrapper collector with pod events, restarts, OOMKills, PVC pressure, and KubectlRead capability gate
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/helm-monitor/src/collectors/kubernetes.rs
  - crates/helm-monitor/src/collectors/mod.rs
autonomous: true
requirements: [REQ-kubernetes-collector]
must_haves:
  truths:
    - "kubectl availability is checked before collector runs"
    - "Pod list is retrieved with event, restart, OOM kill, and PVC pressure information"
    - "KubectlRead capability gate blocks kubectl operations when not authorized"
    - "Collector returns gracefully if kubectl is not installed"
  artifacts:
    - path: crates/helm-monitor/src/collectors/kubernetes.rs
      provides: Kubernetes collector implementation
      exports: ["KubernetesCollector", "KubernetesSnapshot", "PodInfo"]
    - path: crates/helm-monitor/src/collectors/mod.rs
      provides: imports and registry updates
      pattern: "pub use.*kubernetes"
  key_links:
    - from: "KubernetesCollector::collect()"
      to: "bin_exists(kubectl)"
      via: "early-return if missing"
    - from: "collect()"
      to: "run_timed(kubectl, ...)"
      via: "async command execution"
    - from: "kubectl output"
      to: "parse_kubectl()"
      via: "text parsing"
---

<objective>
Implement a Kubernetes collector that wraps kubectl to surface pod metrics: event counts, restart counts, OOMKill events, and PVC pressure signals.

Purpose: Extend HELMOPS to monitor Kubernetes workloads with capability-gated kubectl access.

Output: `crates/helm-monitor/src/collectors/kubernetes.rs` with Collector trait impl, `KubernetesSnapshot` struct, parsing logic, and KubectlRead capability gate.
</objective>

<execution_context>
@.planning/phases/04-m4-kubernetes-vm-compose/04-PATTERNS.md
@crates/helm-monitor/src/collectors/mod.rs
@crates/helm-monitor/src/collectors/containers.rs (analog reference)
</execution_context>

<context>
**Collector trait location:** `crates/helm-monitor/src/collectors/mod.rs` lines 31–42
**Error helper:** `err(domain: &str, msg: impl Into<String>) -> CollectorError` at line 91
**Binary check:** `bin_exists(name: &str) -> bool` at line 82
**Command execution:** `run_timed(program: &str, args: &[&str], profile: MonitorProfile) -> Result<Output>` at line 44
**Analog:** `crates/helm-monitor/src/collectors/containers.rs` (lines 1–48 show exact struct/impl pattern)

**Capability gate decision:** KubectlRead is the permission gate. Check capability before calling kubectl in collect().
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define KubernetesSnapshot and PodInfo structs</name>
  <files>crates/helm-monitor/src/collectors/kubernetes.rs</files>
  <action>
Create `crates/helm-monitor/src/collectors/kubernetes.rs` with:
- `#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]` struct `KubernetesSnapshot { pub pods: Vec<PodInfo>, pub namespace_count: usize, pub available: bool }`
- `pub struct PodInfo { pub namespace: String, pub name: String, pub status: String, pub ready: String, pub restarts: u32, pub age: String, pub events_count: u32, pub oom_kill_count: u32, pub pvc_pressure: bool }`
- Follow snapshot.rs pattern (line 231–264) — use same #[derive] attributes
- No impl blocks yet
  </action>
  <verify>
    <automated>cargo build --lib -p helm-monitor 2>&1 | grep -E "^error|^warning: unused|^Compiling helm-monitor" | head -5</automated>
  </verify>
  <done>File exists with public structs, derives Serialize/Deserialize, compiles without struct-related errors</done>
</task>

<task type="auto">
  <name>Task 2: Implement KubernetesCollector with Collector trait and kubectl integration</name>
  <files>crates/helm-monitor/src/collectors/kubernetes.rs</files>
  <action>
Add to kubernetes.rs:
- `#[derive(Default)] pub struct KubernetesCollector;`
- Implement Collector trait (from mod.rs lines 31–42):
  - `type Output = KubernetesSnapshot;`
  - `fn domain(&self) -> &'static str { "kubernetes" }`
  - `async fn collect(self, profile: MonitorProfile) -> Result<Self::Output, CollectorError>`
- In collect():
  1. Check capability: `if !profile.can_read_kubectl() { return Ok(KubernetesSnapshot::default()); }` (use appropriate capability check from crates/helm-tools/src/capability.rs)
  2. Check `bin_exists("kubectl")` — if false, return `Ok(KubernetesSnapshot { available: false, ... })`
  3. Call `run_timed("kubectl", &["get", "pods", "-A", "-o", "wide"], profile)` with error handling: `Err(e) => return Err(err("kubernetes", e.message))`
  4. Parse stdout via `parse_kubectl()` helper (see next task)
  5. Return `Ok(KubernetesSnapshot { pods, namespace_count: /* count unique namespaces */, available: true })`
- Follow containers.rs lines 6–43 for exact error handling and command structure
  </action>
  <verify>
    <automated>grep -n "impl Collector for KubernetesCollector" crates/helm-monitor/src/collectors/kubernetes.rs && cargo build --lib -p helm-monitor 2>&1 | grep -c "^error"</automated>
  </verify>
  <done>Collector trait impl exists, compiles, collect() method has capability gate + bin_exists check + run_timed call</done>
</task>

<task type="auto">
  <name>Task 3: Implement kubectl output parser</name>
  <files>crates/helm-monitor/src/collectors/kubernetes.rs</files>
  <action>
Add to kubernetes.rs:
- `fn parse_kubectl(s: &str) -> Vec<PodInfo>` following containers.rs lines 49–76 pattern:
  - Split on newlines
  - Filter empty lines
  - For each line, split on whitespace (kubectl -o wide uses space separators)
  - Extract fields: namespace (col 0), name (col 1), ready (col 2), status (col 3), restarts (col 4), age (col 5)
  - Parse restarts as u32, default to 0 on parse error
  - For oom_kill_count and events_count: use secondary kubectl commands (see below) or count from status field heuristically (OOMKilled in status, etc.)
  - Return Vec<PodInfo> with defaults for missing data
- Consider adding a secondary query: `kubectl describe pod -A` or `kubectl get events -A` to enrich OOM/event data (optional for MVP; can be placeholders)
- Use the `err()` helper for any parse failures: `Err(err("kubernetes", format!("parse failed: {}", line)))`
  </action>
  <verify>
    <automated>grep -n "fn parse_kubectl" crates/helm-monitor/src/collectors/kubernetes.rs && cargo build --lib -p helm-monitor 2>&1 | grep -c "^error"</automated>
  </verify>
  <done>parse_kubectl() exists, handles tab/space splitting, extracts all PodInfo fields, handles parse errors gracefully</done>
</task>

<task type="auto">
  <name>Task 4: Register KubernetesCollector in mod.rs and verify gate</name>
  <files>crates/helm-monitor/src/collectors/mod.rs</files>
  <action>
Update `crates/helm-monitor/src/collectors/mod.rs`:
- Line 1–10: Add `pub mod kubernetes;` (alphabetically with other mods)
- Add `pub use kubernetes::KubernetesCollector;` to public exports
- Add `KubectlRead` variant to the `Capability` enum in `crates/helm-core/src/capability.rs` (after the existing variants). Also add its string mapping `"kubectl.read"` to the `as_str()` and `FromStr` impls in that same file.
- The capability gate in the kubernetes collector is binary-level: in `collect()`, call `bin_exists("kubectl")` first; if false, return early with an empty `KubernetesSnapshot::default()` and log a `CollectorError`. This is the standard graceful-degradation pattern used by all collectors.
  </action>
  <verify>
    <automated>grep -n "pub mod kubernetes" crates/helm-monitor/src/collectors/mod.rs && cargo build --lib -p helm-monitor 2>&1 | grep "^error" | wc -l</automated>
  </verify>
  <done>Module exported, compiles without errors</done>
</task>

<task type="auto">
  <name>Task 5: Unit tests for KubernetesCollector</name>
  <files>crates/helm-monitor/src/collectors/kubernetes.rs</files>
  <action>
Add at end of kubernetes.rs:
- `#[cfg(test)] mod tests { ... }`
- Test 1: `parse_kubectl_empty` — pass empty string, expect Vec::new()
- Test 2: `parse_kubectl_single_pod` — pass single pod line, verify PodInfo fields parsed correctly
- Test 3: `parse_kubectl_multiple_namespaces` — verify namespace_count is correct
- Test 4: `bin_exists_false_when_missing` — verify collect() returns default snapshot when kubectl missing (use mock)
- Run: `cargo test -p helm-monitor kubernetes::`
  </action>
  <verify>
    <automated>cargo test -p helm-monitor kubernetes:: --lib 2>&1 | grep -E "test result:|passed"</automated>
  </verify>
  <done>All tests pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>KubernetesCollector struct, Collector trait impl, kubectl integration, output parsing, module registration</what-built>
  <how-to-verify>
1. Run cargo test gate: `cargo fmt --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace --all-targets`
2. Verify kubernetes.rs has exactly these functions: `KubernetesCollector` (struct), `Collector impl`, `parse_kubectl` (fn), tests module
3. Manually test (if kubectl available): `cd /home/white_devil/code/helm && cargo test -p helm-monitor kubernetes:: --lib -- --nocapture`
  </how-to-verify>
  <resume-signal>Type "approved" or describe any failures</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kubectl output → parser | Untrusted text from subprocess; may be malformed or injected |
| Capability gate | KubectlRead permission blocks unauthorized access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|-----------|
| T-04-01 | Tampering | kubectl command injection | mitigate | Use run_timed with fixed args array (no shell interpolation); args hardcoded in code |
| T-04-02 | Information Disclosure | kubectl credentials in output | accept | kubectl runs with current user's context; secrets not logged unless in pod env vars (out of scope) |
| T-04-03 | Elevation of Privilege | KubectlRead gate bypass | mitigate | Capability gate checked before collect() runs; deny if not authorized |
</threat_model>

<verification>
Per-task gate passes before moving to next plan.
</verification>

<success_criteria>
- KubernetesCollector compiles without errors or warnings
- Collector trait impl is complete and matches mod.rs interface
- parse_kubectl() correctly extracts namespace, name, ready, status, restarts, age
- KubectlRead capability gate present and functional
- All unit tests pass
- Full workspace gate passes: fmt + clippy + test
</success_criteria>

<output>
Create `.planning/phases/04-m4-kubernetes-vm-compose/04-01-SUMMARY.md` after task completion with:
- Implementation status (complete/partial)
- Key functions (signatures)
- Files created/modified (paths)
- Next plan dependency (04-02)
</output>
