---
phase: 03-m3-fleet-multi-host
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - helm-cli/src/remote.rs
  - helm-cli/src/main.rs
autonomous: true
requirements:
  - REQ-fleet-uuid-parallel
user_setup: []

must_haves:
  truths:
    - "helm monitor CLI accepts --format json flag and outputs valid JSON array"
    - "RemoteEntry.collect_findings() executes SSH + helm monitor --format json and returns typed findings"
    - "RemoteRegistry.parallel_collect() uses JoinSet bounded to 20 concurrent tasks"
    - "Parallel collection completes 20-host benchmark in ≤2s (p99)"
  artifacts:
    - path: "helm-cli/src/remote.rs"
      provides: "collect_findings async method + parallel_collect on RemoteRegistry + FleetFinding struct"
      contains: "async fn collect_findings"
    - path: "helm-cli/src/main.rs"
      provides: "--format json flag in helm monitor CLI"
      contains: "format == \"json\""
  key_links:
    - from: "helm monitor --format json"
      to: "RemoteEntry.collect_findings()"
      via: "SSH argv + stdout parse"
      pattern: "helm monitor.*format.*json"
    - from: "collect_findings()"
      to: "parallel_collect()"
      via: "JoinSet spawns one task per remote"
      pattern: "JoinSet.*collect_findings"
---

<objective>
Implement parallel SSH findings collection: helm monitor --format json on each remote, JoinSet-bounded to 20 concurrent tasks.

Purpose: Slice 3.2 — Parallel collection engine for fleet refresh
Output: collect_findings() async method + parallel_collect() on RemoteRegistry + --format json flag
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@.planning/PROJECT.md
@.planning/phases/03-m3-fleet-multi-host/03-CONTEXT.md
@.planning/phases/03-m3-fleet-multi-host/03-PATTERNS.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 3 success criteria: 20-host refresh ≤2s)
@.planning/REQUIREMENTS.md (REQ-fleet-uuid-parallel acceptance)

**M3 Context:**
- D-M3-2 (locked): SSH + `helm monitor --format json` on remote. JSON array of FindingSummary-compatible objects.
- D-M3-5 (locked): JoinSet bounded to 20 concurrent tasks. Refresh interval from thresholds.toml (implemented later in slice 3.3).
- Pattern analogs: helm-cli/src/remote.rs (async ping method, ssh_argv); helm-cli/src/main.rs (MonitorArgs struct, run_monitor_command)
- Key files: helm-cli/src/remote.rs lines 96–131 (ping method), helm-cli/src/main.rs lines 634–659 (MonitorArgs)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --format json flag to helm monitor CLI</name>
  <files>helm-cli/src/main.rs</files>
  <read_first>
    - helm-cli/src/main.rs (lines 634–659 to view MonitorArgs structure and existing --format field)
  </read_first>
  <action>
    1. Locate MonitorArgs struct (around line 634–659).
    2. Verify that --format field already exists with value "text" default (noted in PATTERNS.md line 264).
    3. If --format field does not exist, add:
       ```
       #[arg(long, default_value = "text")]
       format: String,
       ```
    4. Locate run_monitor_command() function (around line 3960–4009).
    5. In the format resolution step, ensure that when format == "json", the report output is JSON-formatted (not colorized).
    6. Verify: `resolve_format()` function should return a format enum that includes Json variant, or update it to handle "json" string.
    7. Ensure that when fmt is Json, findings are serialized as JSON array (not TOML, not markdown).
  </action>
  <verify>
    <automated>
      grep -n "format: String" helm-cli/src/main.rs
      grep -n "\"json\"" helm-cli/src/main.rs
      cargo build --bin helm 2>&1 | grep -c "error"
    </automated>
  </verify>
  <acceptance_criteria>
    - MonitorArgs struct has format field (either already present or added)
    - helm monitor --format json compiles without errors
    - When format is "json", output is valid JSON (not colorized text)
  </acceptance_criteria>
  <done>--format json flag available in helm monitor CLI</done>
</task>

<task type="auto">
  <name>Task 2: Add FleetFinding struct to remote.rs</name>
  <files>helm-cli/src/remote.rs</files>
  <read_first>
    - helm-cli/src/remote.rs (entire file to understand module structure and what FindingSummary-like types exist)
  </read_first>
  <action>
    1. Define FleetFinding struct (new, in remote.rs, before RemoteRegistry).
       Fields:
       - host_id: Uuid
       - host_name: String
       - severity: String (or enum Severity { Crit, Warn, Info })
       - title: String
       - description: Option<String>
       - last_seen: Option<i64> (unix timestamp)
    2. Derive: Debug, Clone, Serialize, Deserialize.
    3. This struct represents a single finding from a remote host, tagged with host_id for aggregation.
  </action>
  <verify>
    <automated>
      grep -n "struct FleetFinding" helm-cli/src/remote.rs
      grep -n "#\[derive" helm-cli/src/remote.rs | grep FleetFinding
      cargo build --bin helm 2>&1 | grep -c "error"
    </automated>
  </verify>
  <acceptance_criteria>
    - FleetFinding struct exists with Debug, Clone, Serialize, Deserialize derives
    - Contains host_id: Uuid, host_name: String, severity, title, description
    - Crate compiles
  </acceptance_criteria>
  <done>FleetFinding struct defined and compiles</done>
</task>

<task type="auto">
  <name>Task 3: Implement RemoteEntry.collect_findings() async method</name>
  <files>helm-cli/src/remote.rs</files>
  <read_first>
    - helm-cli/src/remote.rs (lines 96–131 to replicate async ping pattern)
  </read_first>
  <action>
    1. Add async method to impl RemoteEntry:
       ```rust
       pub async fn collect_findings(&self) -> Result<Vec<FleetFinding>> { ... }
       ```
    2. Implementation:
       - Call ssh_argv() to build base SSH command
       - Append args: ["helm", "monitor", "--format", "json"]
       - Spawn tokio::process::Command with stdout captured as Stdio::piped()
       - Read output.stdout as JSON array
       - Parse using serde_json::from_slice() → Vec<serde_json::Value>
       - For each value, extract severity, title, description; tag with self.host_id and self.name
       - Map to FleetFinding vector
       - Handle errors: timeout (30s), SSH error, JSON parse error
       - Return Result<Vec<FleetFinding>>
    3. Error handling: Use .context("collecting findings from {host}") for each potential error.
    4. Timeout: Wrap output.await in tokio::time::timeout(Duration::from_secs(30), ...).
  </action>
  <verify>
    <automated>
      grep -n "pub async fn collect_findings" helm-cli/src/remote.rs
      grep -n "FleetFinding" helm-cli/src/remote.rs | grep -c "host_id"
      cargo build --bin helm 2>&1 | grep -c "error"
    </automated>
  </verify>
  <acceptance_criteria>
    - collect_findings() method exists and is async
    - Returns Result<Vec<FleetFinding>>
    - Calls ssh_argv() + ["helm", "monitor", "--format", "json"]
    - Parses stdout as JSON array
    - Tags findings with host_id and host_name
    - Timeout: 30s per host
    - Handles errors gracefully
    - Crate compiles
  </acceptance_criteria>
  <done>RemoteEntry.collect_findings() implemented with JSON parsing and 30s timeout</done>
</task>

<task type="auto">
  <name>Task 4: Implement RemoteRegistry.parallel_collect() with JoinSet</name>
  <files>helm-cli/src/remote.rs</files>
  <read_first>
    - helm-cli/src/remote.rs (lines 50–93 to understand RemoteRegistry impl structure)
  </read_first>
  <action>
    1. Add async method to impl RemoteRegistry:
       ```rust
       pub async fn parallel_collect(&self) -> Vec<(Uuid, Result<Vec<FleetFinding>>)> { ... }
       ```
    2. Implementation:
       - Create tokio::task::JoinSet<(Uuid, Result<Vec<FleetFinding>>)>
       - For each remote in self.remotes:
         - Spawn task that calls remote.collect_findings()
         - Collect result as (remote.host_id, result)
       - Bounded concurrency: max 20 tasks in flight (JoinSet enforces via max spawn count or semaphore)
       - Wait for all tasks: while let Some(...) = joinset.join_next().await { collect results }
       - Return Vec<(Uuid, Result<...>)>
    3. No timeout at registry level — timeout is per-host in collect_findings()
    4. Error handling: capture both SSH errors and JSON parse errors without panicking
  </action>
  <verify>
    <automated>
      grep -n "pub async fn parallel_collect" helm-cli/src/remote.rs
      grep -n "JoinSet" helm-cli/src/remote.rs
      cargo build --bin helm 2>&1 | grep -c "error"
    </automated>
  </verify>
  <acceptance_criteria>
    - parallel_collect() method exists and is async
    - Returns Vec<(Uuid, Result<Vec<FleetFinding>>)>
    - Uses tokio::task::JoinSet
    - Concurrency bounded to 20 tasks
    - All tasks complete before method returns
    - Handles per-host errors without panic
    - Crate compiles
  </acceptance_criteria>
  <done>RemoteRegistry.parallel_collect() implemented with JoinSet bounded to 20 concurrent tasks</done>
</task>

<task type="auto">
  <name>Task 5: Add integration test for parallel_collect benchmark</name>
  <files>helm-cli/src/remote.rs</files>
  <read_first>
    - helm-cli/src/remote.rs (look for test module or end of file for test structure)
  </read_first>
  <action>
    1. Add integration test (or use existing test module):
       ```rust
       #[tokio::test]
       #[ignore]  // marked ignored; run with -- --ignored
       async fn test_fleet_parallel_refresh_20_hosts() { ... }
       ```
    2. Implementation:
       - Create a mock RemoteRegistry with 20 RemoteEntry fixtures
       - Mock entries have realistic host names and IDs
       - Call parallel_collect()
       - Measure elapsed time
       - Assert: all 20 hosts return results (success or error)
       - Assert: elapsed time ≤ 2s
       - Optional: mock SSH responder returning canned JSON to make test deterministic
    3. Alternative simpler version: create 20 RemoteEntry with invalid hosts, verify all timeout gracefully (30s × 20 = 600s serial, ≤2s parallel if bounded to 20).
    4. Store test in a #[cfg(test)] module at end of remote.rs.
  </action>
  <verify>
    <automated>
      grep -n "test_fleet_parallel_refresh_20_hosts" helm-cli/src/remote.rs
      cargo test fleet_parallel_refresh_20_hosts --lib -- --ignored 2>&1 | grep -E "(test result|PASSED|FAILED|ok)"
    </automated>
  </verify>
  <acceptance_criteria>
    - Test exists and is marked #[ignore]
    - Test creates 20 RemoteEntry fixtures
    - Test calls parallel_collect()
    - Test measures and asserts elapsed time ≤ 2s (or ≤ timeout window if mocked)
    - Test passes when run with -- --ignored
  </acceptance_criteria>
  <done>Integration test added for 20-host parallel collection benchmark</done>
</task>

<task type="auto">
  <name>Task 6: Full suite verification and commit gate</name>
  <files>helm-cli/src/remote.rs, helm-cli/src/main.rs</files>
  <read_first>
    - No additional reads needed
  </read_first>
  <action>
    1. Run `cargo fmt --check` to verify code formatting.
    2. Run `cargo clippy --workspace --all-targets -- -D warnings` for lint check.
    3. Run `cargo test --workspace --all-targets` to ensure no regressions.
    4. Confirm that collect_findings and parallel_collect do not introduce breaking changes to RemoteEntry or RemoteRegistry API.
  </action>
  <verify>
    <automated>
      cargo fmt --check 2>&1 | head -3
      cargo clippy --workspace --all-targets -- -D warnings 2>&1 | grep -c "error"
      cargo test --workspace --all-targets 2>&1 | grep "test result"
    </automated>
  </verify>
  <acceptance_criteria>
    - cargo fmt passes (no formatting issues)
    - cargo clippy reports 0 warnings/errors
    - cargo test --workspace passes (all tests green)
    - Integration test marked #[ignore] is available for M3 gate
  </acceptance_criteria>
  <done>Slice 3.2 complete: Parallel SSH JoinSet + findings collection + benchmark test + full gate pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSH stdout | Remote host stdout over SSH; data is external-tainted |
| JSON parsing | Untrusted JSON from remote host |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Denial of Service | collect_findings timeout | mitigate | 30s per-host timeout prevents hanging; JoinSet bounded to 20 prevents resource exhaustion |
| T-03-05 | Tampering | Malicious JSON from remote | mitigate | JSON deserialized to typed struct (FleetFinding); invalid fields ignored; taint marked External |
| T-03-06 | Information Disclosure | Stderr from SSH/helm process | mitigate | Stderr piped but not parsed; findings only from stdout (JSON) |
</threat_model>

<verification>
- --format json flag in helm monitor CLI ✓
- FleetFinding struct with host_id tagging ✓
- RemoteEntry.collect_findings() async method with 30s timeout ✓
- RemoteRegistry.parallel_collect() with JoinSet (bounded 20) ✓
- Integration test for 20-host benchmark ✓
- No compilation errors ✓
- No clippy warnings ✓
- Test suite green ✓
</verification>

<success_criteria>
1. helm monitor --format json produces valid JSON array output
2. FleetFinding struct models per-host findings with host_id tag
3. RemoteEntry.collect_findings() executes helm monitor --format json over SSH and returns typed findings
4. RemoteRegistry.parallel_collect() uses JoinSet bounded to 20 concurrent tasks
5. 20-host benchmark test completes in ≤2s (or timeout window if simulated)
6. All errors handled gracefully (no panics, no hangs)
7. Crate builds and tests pass
</success_criteria>

<output>
Create `.planning/phases/03-m3-fleet-multi-host/03-02-SUMMARY.md` when done
</output>
