---
phase: 03-m3-fleet-multi-host
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - helm-cli/src/remote.rs
autonomous: true
requirements:
  - REQ-fleet-uuid-parallel
user_setup: []

must_haves:
  truths:
    - "RemoteEntry can specify SSH key identity file via Credential enum"
    - "Backward compatible: existing remotes.toml entries default to SshAgent"
    - "ssh_argv() method correctly injects -i flag for KeyFile variant"
  artifacts:
    - path: "helm-cli/src/remote.rs"
      provides: "Credential enum + RemoteEntry.credential field + ssh_argv update"
      contains: "enum Credential { SshAgent, KeyFile"
  key_links:
    - from: "RemoteEntry struct"
      to: "ssh_argv() method"
      via: "credential field determines -i injection"
      pattern: "self.credential"
---

<objective>
Add credential abstraction to RemoteEntry, enabling per-host SSH key file configuration.

Purpose: Slice 3.1 foundation for parallel SSH collection — each host can use its own key.
Output: Credential enum + RemoteEntry field + ssh_argv extension
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@.planning/PROJECT.md
@.planning/phases/03-m3-fleet-multi-host/03-CONTEXT.md
@.planning/phases/03-m3-fleet-multi-host/03-PATTERNS.md
</execution_context>

<context>
@$HOME/.claude/get-shit-done/references/mandatory-initial-read.md
@.planning/ROADMAP.md (Phase 3 goal + M3 gate)
@.planning/REQUIREMENTS.md (REQ-fleet-uuid-parallel acceptance)

**M3 Context:**
- D-M3-1 (locked): `enum Credential { SshAgent, KeyFile(PathBuf) }` only. No Password. TOML-serializable.
- Pattern analog: helm-cli/src/remote.rs (RemoteEntry struct, ssh_argv method, async error handling)
- Key file: helm-cli/src/remote.rs lines 14–131 (RemoteRegistry, RemoteEntry, ssh_argv, ping)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Credential enum and RemoteEntry field</name>
  <files>helm-cli/src/remote.rs</files>
  <read_first>
    - helm-cli/src/remote.rs (lines 1–150 to understand struct layout, serialization, and ssh_argv context)
  </read_first>
  <action>
    1. After the RemoteEntry struct definition (around line 32), add Credential enum with Debug, Clone, Serialize, Deserialize derives.
       - Variant 1: SshAgent (unit)
       - Variant 2: KeyFile(PathBuf)
    2. Implement Default for Credential, returning Self::SshAgent.
    3. Add field to RemoteEntry struct: `#[serde(default)] pub credential: Credential,` (after ssh_opts field).
    4. Verify TOML serialization works by checking RemoteEntry derives include Serialize/Deserialize.
    5. No changes to RemoteRegistry or other methods yet — just the struct definition.
  </action>
  <verify>
    <automated>
      grep -n "enum Credential" helm-cli/src/remote.rs
      grep -n "pub credential: Credential" helm-cli/src/remote.rs
      grep -n "impl Default for Credential" helm-cli/src/remote.rs
      cargo build --bin helm 2>&1 | grep -c "error"
    </automated>
  </verify>
  <acceptance_criteria>
    - Credential enum exists with SshAgent and KeyFile(PathBuf) variants
    - RemoteEntry struct has credential field with #[serde(default)] attribute
    - Default for Credential returns SshAgent
    - Crate compiles without errors
  </acceptance_criteria>
  <done>Credential enum and RemoteEntry field added and compile clean</done>
</task>

<task type="auto">
  <name>Task 2: Extend ssh_argv() to inject -i for KeyFile credentials</name>
  <files>helm-cli/src/remote.rs</files>
  <read_first>
    - helm-cli/src/remote.rs (lines 96–120 to understand ssh_argv method structure and vector building)
  </read_first>
  <action>
    1. Locate ssh_argv() method (currently around line 96–100).
    2. After the existing ssh_argv() body builds the base argv (ssh, -u user, host, port flags), add credential matching logic:
       - If Credential::SshAgent: no change (sshagent is default)
       - If Credential::KeyFile(path): insert `-i <path>` into argv before the host argument
    3. Example logic:
       ```
       if let Credential::KeyFile(path) = &self.credential {
         argv.insert(argv.len() - 1, "-i".to_owned());
         argv.insert(argv.len() - 1, path.to_string_lossy().to_string());
       }
       ```
    4. Ensure the ordering is correct: ssh [options] -u user -i /path/to/key hostname
    5. Test logic does not break when credential is SshAgent (default).
  </action>
  <verify>
    <automated>
      cargo build --bin helm 2>&1 | grep -c "error"
      cargo test ssh_argv_with_keyfile_injects_identity_flag --lib 2>&1 | grep -E "(test result|PASSED|FAILED)"
    </automated>
  </verify>
  <acceptance_criteria>
    - ssh_argv() compiles without errors
    - When credential is KeyFile(path), argv includes -i and the path
    - When credential is SshAgent, argv does not include -i
    - Unit test ssh_argv_with_keyfile_injects_identity_flag passes
  </acceptance_criteria>
  <done>ssh_argv() method updated to inject -i flag for KeyFile credentials; unit test passes</done>
</task>

<task type="auto">
  <name>Task 3: Add unit test for Credential serialization and ssh_argv integration</name>
  <files>helm-cli/src/remote.rs</files>
  <read_first>
    - helm-cli/src/remote.rs (look for existing #[cfg(test)] module near the end, or examine test structure)
  </read_first>
  <action>
    1. Add a #[cfg(test)] module at the end of remote.rs (or extend existing if present).
    2. Create unit test: `test_ssh_argv_with_keyfile_injects_identity_flag()`
       - Build a RemoteEntry with credential: Credential::KeyFile(PathBuf::from("/home/user/.ssh/id_rsa"))
       - Call ssh_argv()
       - Assert that argv contains "-i" and "/home/user/.ssh/id_rsa" in the correct order
    3. Create unit test: `test_credential_default_is_ssh_agent()`
       - Create RemoteEntry with default credential
       - Assert credential == Credential::SshAgent
    4. Create unit test: `test_credential_serialization_roundtrip()`
       - Serialize a RemoteEntry with KeyFile credential to TOML
       - Deserialize it back
       - Assert credential matches original
  </action>
  <verify>
    <automated>
      cargo test --lib remote::tests --nocapture 2>&1 | grep -E "(test result|passed|failed)"
    </automated>
  </verify>
  <acceptance_criteria>
    - All three unit tests exist and pass
    - Test names clearly indicate what is being verified
    - Tests cover both SshAgent and KeyFile paths
  </acceptance_criteria>
  <done>Unit tests added and passing for Credential abstraction</done>
</task>

<task type="auto">
  <name>Task 4: Full suite verification and commit gate</name>
  <files>helm-cli/src/remote.rs</files>
  <read_first>
    - No additional files; just verify the changes are syntactically clean
  </read_first>
  <action>
    1. Run `cargo fmt --check` on the file to ensure formatting matches project style.
    2. Run `cargo clippy --workspace --all-targets -- -D warnings` to catch any lint issues.
    3. Run `cargo test --workspace --all-targets` to ensure no regressions in other crates.
    4. Confirm that the Credential enum and RemoteEntry changes do not introduce any breaking changes to downstream code.
  </action>
  <verify>
    <automated>
      cargo fmt --check 2>&1 | head -5
      cargo clippy --workspace --all-targets -- -D warnings 2>&1 | grep -c "error"
      cargo test --workspace --all-targets 2>&1 | grep "test result"
    </automated>
  </verify>
  <acceptance_criteria>
    - cargo fmt passes (no formatting issues)
    - cargo clippy reports 0 warnings/errors
    - cargo test --workspace passes (all tests green)
  </acceptance_criteria>
  <done>Slice 3.1 complete: Credential abstraction in RemoteEntry + unit tests + full gate pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Remote host SSH | SSH key path stored in local TOML file; external hosts are remote |
| File system | Key file path from TOML; local file system is trusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | Credential::KeyFile path in TOML | mitigate | remotes.toml stored in config dir (0700 parent, 0600 file if secrets); no debug output of paths |
| T-03-02 | Information Disclosure | SSH key file on disk | mitigate | Key file permissions enforced by OS; HELMOPS only reads via ssh_argv() to SSH agent or ssh -i |
| T-03-03 | Elevation of Privilege | ssh command injection via path | mitigate | PathBuf serialization via TOML prevents injection; ssh args properly split (no shell |)  |
</threat_model>

<verification>
- Credential enum: SshAgent, KeyFile(PathBuf) ✓
- RemoteEntry field: credential with #[serde(default)] ✓
- ssh_argv extension: injects -i <path> for KeyFile ✓
- Unit tests: serialization, default, ssh_argv integration ✓
- No compilation errors ✓
- No clippy warnings ✓
- Test suite green ✓
</verification>

<success_criteria>
1. Credential enum in remote.rs with proper derives and Default impl
2. RemoteEntry.credential field added with serde(default)
3. ssh_argv() correctly injects -i flag when credential is KeyFile
4. Unit tests validate serialization roundtrip and ssh_argv behavior
5. Crate builds and tests pass
6. Backward compatible: existing remotes.toml entries default to SshAgent
</success_criteria>

<output>
Create `.planning/phases/03-m3-fleet-multi-host/03-01-SUMMARY.md` when done
</output>
