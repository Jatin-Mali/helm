---
phase: 03-m3-fleet-multi-host
plan: 05
type: execute
wave: 3
depends_on:
  - "03-01"
files_modified:
  - crates/helm-memory/src/snapshots.rs
  - crates/helm-memory/migrations/0011_host_id_snapshots.sql
autonomous: true
requirements:
  - REQ-fleet-uuid-parallel
user_setup: []

must_haves:
  truths:
    - "Migration 0011 adds host_id TEXT column to snapshots table"
    - "SnapshotRecord struct includes host_id field"
    - "insert() extracts host_id from JSON and stores in snapshots table"
    - "Query functions populate and read host_id field"
  artifacts:
    - path: "crates/helm-memory/migrations/0011_host_id_snapshots.sql"
      provides: "ALTER TABLE snapshots ADD COLUMN host_id TEXT DEFAULT ''"
      contains: "ADD COLUMN host_id"
    - path: "crates/helm-memory/src/snapshots.rs"
      provides: "SnapshotRecord.host_id field + insert/query updates"
      contains: "pub host_id: String"
  key_links:
    - from: "JSON input to insert()"
      to: "host_id column"
      via: "extraction from val[\"host\"][\"id\"]"
      pattern: "host_id.*val.*host.*id"
    - from: "query functions"
      to: "SnapshotRecord.host_id"
      via: "row.get(N)"
      pattern: "row.get.*host_id"
---

<objective>
Add host_id column to snapshots table and update snapshot store to key by both hostname and host_id.

Purpose: Slice 3.5 — DB migration for multi-host keying
Output: Migration 0011 + SnapshotRecord field + insert/query updates
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@.planning/PROJECT.md
@.planning/phases/03-m3-fleet-multi-host/03-CONTEXT.md
@.planning/phases/03-m3-fleet-multi-host/03-PATTERNS.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 3 success criteria: snapshot store keyed by host_id)
@.planning/REQUIREMENTS.md (REQ-fleet-uuid-parallel acceptance)

**M3 Context:**
- Slice 3.5: DB migration 0011_host_id_snapshots.sql — add host_id TEXT column to snapshots table
- Pattern analog: crates/helm-memory/migrations/ (existing migrations like 0006_snapshots.sql), crates/helm-memory/src/snapshots.rs (insert, query functions)
- Key files: crates/helm-memory/migrations/0006_snapshots.sql (table creation), crates/helm-memory/src/snapshots.rs lines 8–95 (struct, insert, query)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration file 0011_host_id_snapshots.sql</name>
  <files>crates/helm-memory/migrations/0011_host_id_snapshots.sql</files>
  <read_first>
    - crates/helm-memory/migrations/0006_snapshots.sql (to understand table schema and index pattern)
  </read_first>
  <action>
    1. Create new file: crates/helm-memory/migrations/0011_host_id_snapshots.sql
    2. Content:
       ```sql
       -- Add host_id column to snapshots table for multi-host keying
       ALTER TABLE snapshots ADD COLUMN host_id TEXT DEFAULT '';
       CREATE INDEX IF NOT EXISTS idx_snapshots_host_id ON snapshots(host_id);
       ```
    3. This migration is idempotent: IF NOT EXISTS on index prevents errors if run multiple times.
    4. Default to empty string for backward compatibility (existing snapshots will have '').
  </action>
  <verify>
    <automated>
      ls -la crates/helm-memory/migrations/0011_host_id_snapshots.sql
      grep -n "ADD COLUMN host_id" crates/helm-memory/migrations/0011_host_id_snapshots.sql
    </automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists at crates/helm-memory/migrations/0011_host_id_snapshots.sql
    - Contains ALTER TABLE ADD COLUMN host_id TEXT DEFAULT ''
    - Contains CREATE INDEX idx_snapshots_host_id
  </acceptance_criteria>
  <done>Migration file 0011_host_id_snapshots.sql created</done>
</task>

<task type="auto">
  <name>Task 2: Add host_id field to SnapshotRecord struct</name>
  <files>crates/helm-memory/src/snapshots.rs</files>
  <read_first>
    - crates/helm-memory/src/snapshots.rs (lines 8–17 to view SnapshotRecord struct)
  </read_first>
  <action>
    1. Locate SnapshotRecord struct (around line 8–17).
    2. Add field after host_hostname:
       ```rust
       pub host_id: String,
       ```
    3. Ensure struct derives Debug, Clone (or verify existing derives include these).
  </action>
  <verify>
    <automated>
      grep -n "pub host_id: String" crates/helm-memory/src/snapshots.rs
      cargo build -p helm-memory 2>&1 | grep -c "error"
    </automated>
  </verify>
  <acceptance_criteria>
    - SnapshotRecord struct includes host_id: String field
    - Crate builds without errors (field will be unused until insert/query updated)
  </acceptance_criteria>
  <done>SnapshotRecord.host_id field added</done>
</task>

<task type="auto">
  <name>Task 3: Update insert() to extract and store host_id</name>
  <files>crates/helm-memory/src/snapshots.rs</files>
  <read_first>
    - crates/helm-memory/src/snapshots.rs (lines 24–49 to view insert() function)
  </read_first>
  <action>
    1. Locate insert() function (around line 24–49).
    2. After extracting host_hostname, add host_id extraction:
       ```rust
       let host_id = val["host"]["id"].as_str().unwrap_or("").to_string();
       ```
    3. Update SQL INSERT statement to include host_id column:
       - Add "host_id" to column list
       - Add "?N" placeholder (where N is the next param index)
    4. Update params![] to include host_id in correct order.
    5. Example:
       ```rust
       conn.execute(
           "INSERT OR REPLACE INTO snapshots (..., host_id, ...) VALUES (..., ?8, ...)",
           params![id, host_hostname, collected_at_ts, profile, domains_json, collector_errors_json, findings_json, host_id],
       )
       ```
  </action>
  <verify>
    <automated>
      grep -n "let host_id = val" crates/helm-memory/src/snapshots.rs
      grep -n "INSERT OR REPLACE.*host_id" crates/helm-memory/src/snapshots.rs
      cargo build -p helm-memory 2>&1 | grep -c "error"
    </automated>
  </verify>
  <acceptance_criteria>
    - insert() extracts host_id from JSON (val["host"]["id"])
    - SQL INSERT includes host_id column and placeholder
    - params![] list includes host_id
    - Crate builds without errors
  </acceptance_criteria>
  <done>insert() function updated to extract and store host_id</done>
</task>

<task type="auto">
  <name>Task 4: Update query functions to populate and read host_id</name>
  <files>crates/helm-memory/src/snapshots.rs</files>
  <read_first>
    - crates/helm-memory/src/snapshots.rs (lines 52–95 to view query functions)
  </read_first>
  <action>
    1. Locate all query functions that return SnapshotRecord (likely get_latest, get_by_id, etc., around lines 52–95).
    2. For each SELECT statement, add host_id to the column list:
       ```sql
       SELECT id, host_hostname, host_id, collected_at, ..., findings_json
       FROM snapshots
       WHERE ...
       ```
    3. In the row mapper, add mapping for host_id:
       ```rust
       host_id: row.get(2)?  // or appropriate column index
       ```
    4. Adjust other row.get() indices if necessary due to new column position.
    5. Ensure all query functions that return SnapshotRecord are updated.
  </action>
  <verify>
    <automated>
      grep -n "host_id: row.get" crates/helm-memory/src/snapshots.rs | wc -l
      grep -n "SELECT.*host_id" crates/helm-memory/src/snapshots.rs | wc -l
      cargo build -p helm-memory 2>&1 | grep -c "error"
    </automated>
  </verify>
  <acceptance_criteria>
    - All SELECT statements include host_id column
    - Row mappers include host_id: row.get(N) for each query function
    - All query functions that return SnapshotRecord are updated
    - Crate builds without errors
  </acceptance_criteria>
  <done>Query functions updated to populate and read host_id field</done>
</task>

<task type="auto">
  <name>Task 5: Add unit test for host_id roundtrip</name>
  <files>crates/helm-memory/src/snapshots.rs</files>
  <read_first>
    - crates/helm-memory/src/snapshots.rs (look for test module or end of file)
  </read_first>
  <action>
    1. Add test to #[cfg(test)] module:
       ```rust
       #[test]
       fn test_host_id_roundtrip() {
           // Create test SnapshotRecord with host_id = "uuid-12345"
           // Insert into in-memory SQLite DB
           // Query back by ID
           // Assert host_id matches original
       }
       ```
    2. Alternatively, test multiple hosts:
       ```rust
       #[test]
       fn test_multiple_hosts_snapshots() {
           // Insert snapshot for host_id = "uuid-1"
           // Insert snapshot for host_id = "uuid-2"
           // Query all snapshots
           // Assert correct host_id for each
       }
       ```
  </action>
  <verify>
    <automated>
      grep -n "test_host_id_roundtrip\|test_multiple_hosts" crates/helm-memory/src/snapshots.rs
      cargo test -p helm-memory host_id 2>&1 | grep -E "(test result|PASSED|FAILED|ok)"
    </automated>
  </verify>
  <acceptance_criteria>
    - Unit test exists for host_id roundtrip
    - Test inserts and retrieves snapshot with host_id
    - Test validates host_id matches original
    - Test passes
  </acceptance_criteria>
  <done>Unit test added for host_id storage and retrieval</done>
</task>

<task type="auto">
  <name>Task 6: Full suite verification and commit gate</name>
  <files>crates/helm-memory/src/snapshots.rs, crates/helm-memory/migrations/0011_host_id_snapshots.sql</files>
  <read_first>
    - No additional reads needed
  </read_first>
  <action>
    1. Run `cargo fmt --check` to verify code formatting.
    2. Run `cargo clippy --workspace --all-targets -- -D warnings` for lint check.
    3. Run `cargo test --workspace --all-targets` to ensure no regressions in helm-memory and other crates.
    4. Confirm migration file is syntactically valid SQL (can be verified by running against test DB).
  </action>
  <verify>
    <automated>
      cargo fmt --check 2>&1 | head -3
      cargo clippy --workspace --all-targets -- -D warnings 2>&1 | grep -c "error"
      cargo test --workspace --all-targets 2>&1 | grep "test result"
    </automated>
  </verify>
  <acceptance_criteria>
    - cargo fmt passes (no formatting issues)
    - cargo clippy reports 0 warnings/errors
    - cargo test --workspace passes (all tests green)
  </acceptance_criteria>
  <done>Slice 3.5 complete: DB migration + host_id field + roundtrip tests + full gate pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Database | SQLite DB is local, trusted |
| JSON input | External JSON from remote hosts (processed by insert()) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Tampering | host_id column in snapshots | mitigate | host_id extracted from trusted JSON; stored in DB; no user input |
| T-03-11 | Information Disclosure | host_id in DB queries | accept | host_id is non-sensitive host identifier; DB is local and 0600 mode |
</threat_model>

<verification>
- Migration 0011_host_id_snapshots.sql created ✓
- SnapshotRecord.host_id field added ✓
- insert() extracts and stores host_id ✓
- Query functions populate and read host_id ✓
- Unit test for roundtrip ✓
- No compilation errors ✓
- No clippy warnings ✓
- Test suite green ✓
</verification>

<success_criteria>
1. Migration 0011_host_id_snapshots.sql adds host_id TEXT column to snapshots table
2. SnapshotRecord struct includes host_id: String field
3. insert() extracts host_id from JSON and stores in DB
4. All query functions populate host_id field from row data
5. Unit test validates host_id storage and retrieval
6. Crate builds and tests pass
7. Backward compatible: existing snapshots have empty host_id
</success_criteria>

<output>
Create `.planning/phases/03-m3-fleet-multi-host/03-05-SUMMARY.md` when done
</output>
